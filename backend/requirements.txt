# backend/requirements.txt
# Version lower bounds are minimum compatible with Python 3.11.
# For production: compile exact pins with pip-compile or uv pip compile.

Django>=4.2,<5.0
djangorestframework>=3.15,<4.0
django-environ>=0.11
django-cors-headers>=4.3
psycopg2-binary>=2.9
# NOTE: Replace psycopg2-binary with psycopg2 in production deployments.
# psycopg2-binary bundles PostgreSQL client libs; security patches lag.
httpx>=0.27
cryptography>=42